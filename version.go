package qshape

// GroupingVersion identifies the clustering and fingerprinting rules.
// Bump it in any change that moves a Fingerprint value or moves a query
// between clusters.
const GroupingVersion = 1
